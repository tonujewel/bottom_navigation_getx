
import 'package:flutter/material.dart';

class Style {


  static Color primaryColor = Color(0xffa29bfe);

  static BoxShadow boxShadow = BoxShadow(
    color: Colors.grey.withOpacity(0.5),
    spreadRadius: 5,
    blurRadius: 7,
    offset: Offset(0, 3), // changes position of shadow
  );


}